export interface navItemsType {
  name: string;
  path: string;
}

export interface bannerDataType {
  imgUrl: string;
  title: string;
  smallText: string;
  bigText: string;
  buttonText: string;
  description: string;
}

export interface productDataType {
  id: string;
  imgUrl: string;
  title: string;
  slug: string;
  price: number;
  description: string;
}
